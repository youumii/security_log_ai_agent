# 포트폴리오 설명 가이드

## 한 줄 소개

> Python과 Flask를 활용하여 인증 로그의 비정상 로그인 패턴을 탐지하고,
> 위험도 평가·AI 보조 분석·자동 보고서 생성을 수행하는 보안 로그 분석 시스템을 개발했습니다.

## 문제 정의

서버 인증 로그는 데이터가 많아 사람이 직접 반복적인 로그인 실패나
여러 계정을 대상으로 한 공격 패턴을 빠르게 찾기 어렵습니다.

이를 자동화하기 위해 로그를 구조화하고 시간 기반 규칙을 적용하여
확인 우선순위가 높은 이벤트를 자동 분류하는 시스템을 구현했습니다.

## 내가 구현한 핵심

- CSV/SSH 인증 로그 파서
- 잘못된 로그 예외 처리
- Sliding time window 방식의 반복 로그인 실패 탐지
- 사용자 기준 / IP 기준 교차 분석
- 로그인 실패 후 성공 패턴 탐지
- 위험도 점수화
- Flask 웹 대시보드
- TXT/JSON 보고서 생성
- LLM 선택 연동
- HIGH 위험 Webhook 알림
- unittest 기반 자동 테스트

## 발표에서 보여줄 순서

1. `sample_normal_logs.csv` 업로드
   - LOW
   - 탐지 이벤트 없음

2. `sample_attack_logs.csv` 업로드
   - HIGH
   - Brute Force 탐지
   - 동일 IP 다중 계정 공격 탐지
   - 실패 후 성공 탐지

3. 시간대별 실패 그래프 설명

4. AI 분석 요약 설명

5. TXT/JSON 보고서 다운로드

## 예상 질문

### 왜 AI가 직접 공격을 판단하지 않았나요?

탐지 결과의 재현성과 설명 가능성을 높이기 위해
핵심 탐지는 Python 규칙 엔진으로 수행했습니다.
LLM은 구조화된 탐지 결과를 자연어로 요약하는 역할만 담당하도록 설계했습니다.

### 왜 단순 실패 횟수 대신 시간 조건을 넣었나요?

하루 동안 로그인 실패가 5번 발생한 것과
30초 동안 5번 발생한 것은 보안적으로 의미가 다르기 때문입니다.
따라서 sliding time window를 사용해 짧은 시간에 집중된 실패를 탐지했습니다.

### 종합 위험도 점수는 어떻게 계산했나요?

종합 위험도를 공격 강도와 영향도 두 축으로 나눴습니다. 공격 강도는 60초 내 로그인 실패 밀도와 동일 IP의 공격 대상 계정 수를 단계적으로 점수화하고, 영향도는 실패 후 성공이나 성공 IP 변화 같은 침해 가능성 징후를 반영합니다.

```text
종합 위험도 = 공격 강도(0~50) + 영향도(0~50)
```

전체 로그 건수로 나누지 않는 이유는 로그가 길다는 이유만으로 중요한 공격 패턴이 희석될 수 있기 때문입니다.

### 오탐 가능성은 없나요?

있습니다. 반복 로그인 실패는 사용자의 비밀번호 입력 실수일 수도 있습니다.
따라서 결과를 '공격 확정'으로 표시하지 않고 위험도와 탐지 근거를 제공해
관리자가 추가 확인하도록 설계했습니다.

## 이력서용 문장

> Python/Flask 기반 보안 로그 분석 웹 애플리케이션 개발.
> 시간 기반 Brute Force 및 다중 계정 접근 패턴을 탐지하고,
> 위험도 점수화·LLM 보조 분석·TXT/JSON 자동 보고 기능을 구현.

## GitHub README에 추가하면 좋은 자료

- 메인 화면 스크린샷
- 정상 로그 분석 결과 스크린샷
- 공격 로그 분석 결과 스크린샷
- 시스템 구성도
- 탐지 규칙 설명
- 개선 전/후 비교


## 문제 해결 사례 — 위험도 점수 체계 통합

초기 버전에서는 개별 탐지 이벤트마다 별도의 위험 점수와 등급을 부여했습니다.
이후 종합 위험도를 가중치 합산 방식으로 변경하면서 다음과 같은 모순 가능성을 발견했습니다.

```text
개별 이벤트: HIGH
종합 위험도: LOW
```

같은 로그에서 서로 다른 점수 체계가 동시에 노출되면 사용자가 결과를 이해하기 어렵다고 판단했습니다.

### 개선

- 개별 이벤트의 점수/등급 제거
- 개별 이벤트는 탐지 사실과 근거만 표시
- 점수와 LOW/MEDIUM/HIGH 등급은 종합 위험도에서만 계산
- 종합 위험도는 가중치 산정 근거를 별도로 펼쳐 확인 가능

### 설명할 때

> 초기에는 개별 탐지와 종합 위험도에 각각 점수를 부여했지만,
> 테스트 과정에서 두 점수 체계가 서로 다른 위험 등급을 보여줄 수 있다는 문제를 발견했습니다.
> 그래서 개별 이벤트는 탐지 근거에 집중하고, 최종 점수는 하나의 가중치 모델로 통합했습니다.



## 구현 중 발생한 오류 — 데이터 구조 변경 후 KeyError

개별 탐지 이벤트의 `score` 필드를 제거한 뒤,
AI 요약 코드가 예전 구조인 `top["score"]`를 계속 참조해 `KeyError`가 발생했습니다.

### 원인

```text
core.py
개별 이벤트 score 제거
        ↓
ai_summary.py
기존 top["score"] 참조가 남아 있음
        ↓
KeyError: 'score'
```

### 해결

AI 요약은 개별 이벤트 점수가 아니라
`overall_risk["score"]`에 저장된 종합 위험도만 사용하도록 수정했습니다.

또한 이후에는 단위 테스트뿐 아니라 Flask의 실제 `/analyze` 업로드 요청까지
테스트하여 화면 렌더링 단계의 오류도 확인하도록 검증 범위를 넓혔습니다.



## 문제 해결 사례 — 위험 점수 포화 문제 개선

긴 테스트 로그를 분석하면서 여러 탐지 규칙이 동시에 충족될 경우 단순 가중치 합산 방식의 점수가 빠르게 HIGH 상단까지 올라가는 문제를 발견했습니다.

처음에는 전체 로그 수로 나누는 방식을 고려했지만, 로그가 길다는 이유만으로 중요한 공격 징후의 위험도가 낮아질 수 있어 적절하지 않다고 판단했습니다.

### 개선

```text
공격 강도 0~50
- 짧은 시간 내 로그인 실패 밀도
- 동일 IP가 공격한 계정 범위

영향도 0~50
- 반복 실패 후 로그인 성공
- 성공 IP 변화
- 여러 출처에서 동일 계정 접근
- 복수 계정의 실패 후 성공
```

공격 강도는 실패 횟수나 대상 계정 수가 늘어날 때 단계적으로 점수가 올라가도록 하여 단순 이진 가중치보다 세밀하게 표현했습니다.

### 설명할 때

> 초기에는 탐지 요소별 가중치를 단순 합산했지만, 긴 로그에서 여러 패턴이 동시에 발견되면 점수가 빠르게 포화되는 문제를 확인했습니다. 전체 로그 수로 나누면 실제 공격을 과소평가할 수 있어, 공격의 밀도와 범위를 나타내는 '강도'와 실제 침해 가능성을 나타내는 '영향도'로 분리해 점수 모델을 재설계했습니다.


## 구현 중 발생한 오류 — Jinja `items` 이름 충돌

위험도 모델을 `공격 강도`와 `영향도`로 분리하면서
다음과 같이 상세 항목 목록을 `items`라는 키에 저장했습니다.

```python
{
    "intensity": {
        "items": [...]
    }
}
```

템플릿에서 이를 다음처럼 접근했습니다.

```jinja2
result.overall_risk.intensity.items
```

하지만 Jinja에서는 `dict.items`가 파이썬 딕셔너리의 `items()` 메서드로 해석될 수 있어
목록 대신 메서드를 반복하려 하면서 TypeError가 발생했습니다.

### 해결

키 접근을 명확한 bracket notation으로 변경했습니다.

```jinja2
result.overall_risk.intensity["items"]
result.overall_risk.impact["items"]
```

데이터 키 이름이 파이썬 객체의 메서드명과 겹칠 수 있다는 점을 확인한 사례입니다.


## UI 개선 — 시간대별 로그인 실패 그래프 간소화

긴 로그에서는 실패가 없는 시간대까지 그래프에 표시되어 가로 길이가 지나치게 길어지는 문제가 있었습니다.

그래프를 로그인 실패가 1회 이상 발생한 1분 구간만 표시하도록 수정했습니다.
이 변경은 시각화에만 적용하며 Sliding Window 기반 탐지와 종합 위험도 계산은 원본 로그를 그대로 사용합니다.
